"""HypoKiln agents.

Each agent is a folder with one `instructions.md` (the system prompt).
There are no Python agent classes — the CLI runner reads instructions.md
directly and spawns codex/claude/gemini as a subprocess.
"""
